/**
 * Creates scheduler jobs for setting bank power
 */
const cron = require('node-cron');

const config = require('../models/config.js');

module.exports = {};

module.exports.init = (callback) => {
  // TODO
  callback(null);
};
